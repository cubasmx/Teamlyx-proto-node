services:
  teamlyx-backend:
    build: .
    container_name: teamlyx-checador
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
